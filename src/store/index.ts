import { createStore } from 'vuex'

export default createStore({
  state: {
    openLoginForm: false,
    closeLoginForm: false
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
